import express, { Request, Response, NextFunction } from 'express';
const router = express.Router();


router.put('/reserve');
router.post('/check-in')
router.get('/review-listings')

export default router;